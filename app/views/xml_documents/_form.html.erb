<%= form_with(model: xml_document) do |form| %>
  <% if xml_document.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(xml_document.errors.count, "error") %> prohibited this xml_document from being saved:</h2>

      <ul>
        <% xml_document.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :s3, style: "display: block" %>
    <%= form.text_area :s3 %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
